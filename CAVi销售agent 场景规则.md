# CAVi 销售 Agent 场景规则

> **本文件用途**：存档 CAVi 销售 Agent 的所有业务场景规则
> **范围**：仅记录**底层处理规则**（CRM 状态机、CAVi Agent 行为、ycloud n8n 触发逻辑）
> **不包含**：UI 可视化设计（这部分用 HTML 原型实现，见 `统一客服收件箱-PC.html`）
>
> **版本**：v0.1 · 2026-09-01
> **配套文档**：
> - `WhatsApp Agent/CAVi AI Sales Agent 主动跟进规划.md`（v0.4 业务全景）
> - `销售agent/统一客服收件箱-PC.html`（销售可视化操作界面）

---

## 一、场景分类原则

每个场景标注 3 个属性：

| 属性 | 取值 | 含义 |
|---|---|---|
| **类型** | A 底层规则 / B 可视化功能 / C 独立页面 | 落地形式 |
| **触发主体** | CRM / CAVi / ycloud / Meta / 销售 / 客户 | 谁发起 |
| **优先级** | P0 / P1 / P2 | 实现紧迫度 |

---

## 二、场景全量清单（62 个）

按 7 个维度组织：**会话来源 → 会话主体 → 客户行为 → 时间维度 → 内容形态 → 团队协作 → 合规风控**

### 维度 ① 会话来源（8 个）

#### 场景 1：客户留资触发 AI 主动跟进
- **类型**：A（CAVi 自动处理）
- **触发主体**：CRM lead 创建 → CAVi 监听 → ycloud n8n Webhook
- **触发条件**：autocava.com.mx 表单提交 → lead.status = new
- **Agent 行为**：
  1. CAVi 判断是否符合主动跟进条件（黑名单 / 已成交 / 已拒绝）
  2. 通过 ycloud WA API 发送 HSM 模版（首联问候 + 询问意向）
  3. 设置 AI 接管会话，状态：`ai_outreach_pending`
  4. 进入 24h 客服窗口倒计时
- **CRM 状态变更**：`new` → `ai_contacted`
- **UI 反馈**：会话项显示 `🤖 AI 接管`标签（**已有**）

#### 场景 2：陌生用户 WA 主动找上门
- **类型**：A
- **触发主体**：ycloud Webhook → CAVi → CRM
- **触发条件**：收到陌生 phone 的 inbound 消息（无既有 lead 匹配）
- **Agent 行为**：
  1. CAVi 调 CRM 匹配 phone
  2. 不匹配 → 自动新建 lead（source = `wa_inbound_only`，status = `inbound_only`）
  3. CAVi 启动轻量 AI 询问（采集意向 + 引导留资）
  4. 客户同意 → 升级为常规 lead；拒绝 → 标记低意向继续 AI 跟进
- **CRM 状态变更**：新建 lead（`inbound_only`） → `inbound_qualifying` → 转场景1 后续
- **UI 反馈**：会话项显示 `🆕 陌生 lead` 紫色标签（**缺失**，待补）

#### 场景 3：电话回呼留 WA
- **类型**：B
- **触发主体**：销售手动操作
- **触发条件**：客户电话咨询后，销售在 CRM 录入客户 phone 并勾选"已沟通"
- **Agent 行为**：
  1. CAVi 收到 CRM 事件，触发 HSM 模版（电话后续 WA 问候）
  2. 进入 AI 接管
- **CRM 状态变更**：`phone_consulted` → `ai_following`
- **UI 反馈**：会话项显示 `📞 客户来源：电话` 标签（**缺失**，待补）

#### 场景 4：展厅到店后 WA 跟进
- **类型**：A
- **触发主体**：CRM（门店签到系统）
- **触发条件**：门店 POS / 客流系统标记客户到店 + 浏览某车型
- **Agent 行为**：
  1. CAVi 等待销售手动决定是否 WA 跟进（不自动打扰）
  2. 销售在 CRM 触发 `启动跟进` → CAVi 发送 HSM
- **CRM 状态变更**：`visited` → `sales_initiated` → `ai_following`
- **UI 反馈**：profile 显示 `🏪 到店记录：2026-08-30 XPENG G6`（**缺失**）

#### 场景 5：老客户复购 / 置换
- **类型**：B
- **触发主体**：CRM（成交记录）
- **触发条件**：phone 匹配既有已成交客户
- **Agent 行为**：
  1. CAVi 拉取客户历史购车记录
  2. 启动"复购场景" HSM（"感谢您选择 XPENG G6，是否考虑升级置换？"）
  3. AI 询问置换需求
- **CRM 状态变更**：`existing_customer` → `repurchase_qualifying`
- **UI 反馈**：profile 顶部 `🔁 老客户复购` 徽章 +购车历史区块（**缺失**）

#### 场景 6：转介绍
- **类型**：B
- **触发主体**：销售手动 + CRM
- **触发条件**：销售在 CRM 录入 lead 时标记 `referrer_phone`
- **Agent 行为**：
  1. CAVi 关联推荐人 lead
  2. AI 启动"转介绍场景" HSM（"X 先生/女士 推荐您了解..."）
  3. 给推荐人发感谢 HSM
- **CRM 状态变更**：新 lead `referred_by_X` → `ai_following`
- **UI 反馈**：profile 显示 `👥 来源：王女士介绍`（**缺失**）

#### 场景 7：同 lead 多人联系（multi-party）
- **类型**：A
- **触发主体**：ycloud Webhook
- **触发条件**：同一 lead 关联的 phone 出现多个不同 phone
- **Agent 行为**：
  1. CAVi 检测到多个 phone
  2. 创建 multi-party chat session（合并 / 分流决策由规则引擎）
  3. 群发场景下启用多 phone 同步消息
- **CRM 状态变更**：新增 `linked_phones` 字段
- **UI 反馈**：会话项显示 `👥 3 人沟通中` 头像组（**缺失**）

#### 场景 8：跨平台迁移（FB/IG → WA）
- **类型**：A
- **触发主体**：CRM lead 来源字段
- **触发条件**：CRM 已有 FB/IG lead 来源，现在 WA 也激活
- **Agent 行为**：
  1. CAVi 拉取 FB/IG 历史对话摘要
  2. WA 对话首条引用 FB 历史（"我看到您之前在 FB 咨询了..."）
- **CRM 状态变更**：`cross_channel_active`
- **UI 反馈**：profile 显示 `🌐 历史渠道：FB Messenger`（**缺失**）

---
### 维度 ② 会话主体（6 个）

#### 场景 9：AI 单方主动外呼（HSM 首联）
- **类型**：B
- **触发主体**：CAVi → ycloud n8n
- **触发条件**：HSM 模版已发送，等待客户回复
- **Agent 行为**：
  1. CAVi 监控 WA delivery status
  2. 已送达 → 启动 24h 倒计时
  3. 客户未回复 → 状态 `ai_outreach_pending`（不动）
  4. 客户回复 → 立即切到 AI 对话模式
- **CRM 状态变更**：`ai_outreach_sent`
- **UI 反馈**：会话项 `📤 AI 外呼中，等待回复` 标签 + 倒计时（**缺失**）

#### 场景 10：AI ↔ 客户对话
- **类型**：B（**已有**）
- **触发主体**：CAVi NLU
- **Agent 行为**：
  1. 客户每次 inbound → CAVi 解析意图
  2. 自动回复 / 收集资料 / 引导流程
  3. 触发 Handoff 条件 → 切销售接管
- **UI 反馈**：`🤖 AI 接管` 标签 + 消息流

#### 场景 11：销售 ↔ 客户对话
- **类型**：B（**已有**）
- **触发主体**：Handoff 触发 / 销售主动接管
- **Agent 行为**：
  1. 锁定会话为 `sales_handling`
  2. AI 暂停回复
  3. 启动销售 SLA 监控（场景27）
- **UI 反馈**：`✅ 转人工` 标签

#### 场景 12：AI + 销售协作
- **类型**：A
- **触发主体**：CAVi 内部 routing
- **触发条件**：客户消息触发"专业问题"分类（金融 / 法务 / 售后）
- **Agent 行为**：
  1. CAVi 自动 `@mention` 内部专家（金融顾问 / 法务）
  2. AI 答一般问题，销售 / 专家答专业问题
  3. 内部对话和客户对话隔离
- **CRM 状态变更**：`collaborative_handling`
- **UI 反馈**：会话项 `🤝 AI + 销售协作中`（**缺失**）

#### 场景 13：多销售协作同一客户
- **类型**：B
- **触发主体**：CRM ownership 字段
- **触发条件**：主销售休假 / 离职 / 主动转派
- **Agent 行为**：
  1. CRM 记录 `co_owners` 列表
  2. 多个销售可查看 / 回复同一 lead
  3. 主销售回复时标记"主销售已回复"，副销售进入旁观模式
- **CRM 状态变更**：`co_owned`
- **UI 反馈**：会话项 `👥 主：张三 / 副：李四` + 旁观模式标识（**缺失**）

#### 场景 14：销售 + 主管介入
- **类型**：B + C（独立页面）
- **触发主体**：销售手动 + 主管手动
- **触发条件**：谈判进入价格阶段 / 主管主动质检
- **Agent 行为**：
  1. CAVi 检测到价格谈判关键词 → 提示销售"是否请求主管协助"
  2. 销售发起 → 主管介入 session
  3. 主管接管对话（销售进入旁观）
- **CRM 状态变更**：`manager_escalated`
- **UI 反馈**：chat header `👔 主管已介入（王经理）`（**缺失**）
- **独立页面**：`主管质检视图-PC.html`（待造）

---
### 维度 ③ 客户行为 / 意图（12 个）

#### 场景 15：客户明确表达购车意向
- **类型**：B（**已有**）
- **触发条件**：客户说"我想买 / 我要订 / 怎么付款"
- **Agent 行为**：CAVi 触发 Handoff，立即切销售接管
- **UI 反馈**：🔴 待销售接管

#### 场景 16：客户表达贷款需求
- **类型**：A
- **触发主体**：CAVi NLU
- **触发条件**：客户提到 "crédito / financiamiento / 贷款 / 月供"
- **Agent 行为**：
  1. CAVi 打 `loan_intent` 标签
  2. AI 自动回复贷款基础问题
  3. 资料收集流程启动
- **CRM 状态变更**：`loan_intent_confirmed`
- **UI 反馈**：profile `AI 标签：loan_intent`

#### 场景 17：客户犹豫不决
- **类型**：A
- **触发主体**：CAVi 意图分类
- **触发条件**：客户说"我再想想 / 让我考虑下 / 跟家人商量"
- **Agent 行为**：
  1. CAVi 打 `hesitant` 标签
  2. 24h 后自动发 HSM 唤醒（"还在考虑 XPENG G6 吗？"）
  3. 48h 后仍未回 → 标记 `cold_lead`
- **CRM 状态变更**：`hesitant` → `cold_lead`（48h 无回复）
- **UI 反馈**：会话项 `🤔 犹豫中` 灰色标签（**缺失**）

#### 场景 18：客户已对比竞品
- **类型**：A
- **触发主体**：CAVi NLU + 实体识别
- **触发条件**：客户提到竞品名（"VW Tiguan / Nissan Versa / MG5"）
- **Agent 行为**：
  1. CAVi 提取竞品实体
  2. 打 `competitor_mentioned` 标签
  3. AI 准备差异化话术（不诋毁竞品，只讲 XPENG 优势）
- **UI 反馈**：profile `对比竞品：VW Tiguan, MG5`（**缺失**）

#### 场景 19：客户压价/砍价
- **类型**：A
- **触发主体**：CAVi 价格意图 NLU
- **触发条件**：客户说"能不能便宜 / 有优惠吗 / descuento"
- **Agent 行为**：
  1. CAVi 打 `price_negotiation` 标签
  2. AI 不直接答"能便宜多少"（避免给底价）
  3. 转销售接管（销售有底价权限）
- **UI 反馈**：会话项 `💰 议价中` 标签（**缺失**）

#### 场景 20：客户要求试驾
- **类型**：B
- **触发主体**：CAVi NLU
- **触发条件**：客户说"能试驾吗 / test drive / 试乘"
- **Agent 行为**：
  1. CAVi 打 `test_drive_request` 标签
  2. AI 询问意向时间 + 门店
  3. 调 CRM 排期（销售跟进确认）
- **CRM 状态变更**：`test_drive_pending`
- **UI 反馈**：chat input 快捷按钮 `🚗 预约试驾`（**缺失**）

#### 场景 21：客户索要贷款资料清单
- **类型**：B（**已有**）
- **触发条件**：客户说"需要什么资料 / documentos"
- **Agent 行为**：AI 自动回复清单（INE / 收入证明 / 银行流水）
- **UI 反馈**：profile 资料清单区块

#### 场景 22：客户上传资料图片
- **类型**：B
- **触发主体**：ycloud Webhook 媒体消息
- **触发条件**：客户发送图片
- **Agent 行为**：
  1. CAVi OCR 识别（INE / 收入证明）
  2. 自动归档到 CRM 客户 documents
  3. 更新 `资料状态` 字段
- **CRM 状态变更**：`documents_uploading`
- **UI 反馈**：消息流 `📎 客户已上传 INE` 卡片 + OCR 状态（**缺失**）

#### 场景 23：客户突然失联
- **类型**：B
- **触发主体**：CRM 调度任务
- **触发条件**：客户最后 inbound 后 7+ 天无回复
- **Agent 行为**：
  1. CRM 标记 `lost_contact`
  2. CAVi 启动 3 阶段唤醒 HSM（D+7 / D+14 / D+30）
  3. 全部无回复 → 归档到 cold_lead 池
- **CRM 状态变更**：`lost_contact` → `cold_lead`
- **UI 反馈**：会话项 `👻 失联 7 天` 标签 + 新 Tab `失联池`（**缺失**）

#### 场景 24：客户投诉 / 不满
- **类型**：B
- **触发主体**：CAVi 负面情绪检测
- **触发条件**：客户消息情绪评分 < -0.6
- **Agent 行为**：
  1. CAVi 立即打 `complaint` 标签（最高优先级）
  2. AI 自动安抚话术（"非常抱歉给您带来困扰..."）
  3. 通知主管（站内 / 邮件）
  4. 立即切销售接管
- **CRM 状态变更**：`complaint_received`
- **UI 反馈**：会话项 `😡 投诉` 红色标签 + 升级主管按钮（**缺失**）

#### 场景 25：客户质疑真实性
- **类型**：A
- **触发主体**：CAVi 信任风险 NLU
- **触发条件**：客户说"真的假的 / 骗人 / scam"
- **Agent 行为**：
  1. CAVi 打 `trust_concern` 标签
  2. AI 提供官方资料链接（autocava.com.mx 官网）
  3. 邀请客户到店看实车
- **UI 反馈**：profile 备注 `⚠️ 信任疑虑`（**缺失**）

#### 场景 26：客户群发比价
- **类型**：A
- **触发主体**：CAVi 对话频次分析
- **触发条件**：同一客户短时间内与多家经销商对话（通过内容相似度 + 时间分布检测）
- **Agent 行为**：
  1. CAVi 打 `multi_vendor` 标签
  2. 启动差异化策略（强调 XPENG 独有优势：电池 / 智能驾驶 / OTA）
- **UI 反馈**：profile `⚖️ 客户正在比价`（**缺失**）

---
### 维度 ④ 时间维度（10 个）

#### 场景 27：销售 idle >1h（用户核心场景）
- **类型**：A + B
- **触发主体**：CRM SLA 监控
- **触发条件**：
  - 会话状态 = `sales_handling`
  - 客户最后 inbound 时间 > 1h
  - 销售未发出 outbound
- **Agent 行为**：
  1. CRM 启动 idle 计时器
  2. 1h 阈值 → 标记 `sales_idle_warning`
  3. 2h 阈值 → 触发自动升级选项：
     - **选项 A**：自动让 AI 接管续接（推荐）
     - **选项 B**：转派其他销售
     - **选项 C**：通知主管
  4. 4h 阈值 → 自动让 AI 接管（不需销售确认）
- **CRM 状态变更**：`sales_handling` → `sales_idle` → `ai_recovery`（如 AI 接管）
- **UI 反馈**：
  - 会话项黄色高亮
  - chat header `⏰ 销售 idle 1h 12min`
  - chat actions `🤖 AI 续接` 按钮 + `📤 转派` 按钮
  - 新增 Tab `销售 idle`（**缺失，P0**）

#### 场景 28：AI 等客户回复 >30min
- **类型**：A
- **触发主体**：CAVi 内部 timer
- **触发条件**：AI 最后 outbound 后 30min 客户未答
- **Agent 行为**：
  1. CAVi 标记 `awaiting_customer`
  2. 不主动重发（避免打扰）
  3. 等待 24h 窗口结束
- **UI 反馈**：会话项 `⏳ 等待客户回复` 灰色（**缺失**）

#### 场景 29：AI 等客户回复 >2h
- **类型**：A
- **触发主体**：CAVi 内部 timer
- **触发条件**：客户最后 inbound >2h
- **Agent 行为**：
  1. CAVi 评估是否需要 AI 主动续接
  2. 是 → AI 续接一句（"还在考虑吗？"）
  3. 否 → 标记 `awaiting_long`
- **UI 反馈**：会话项 `⏰ 待跟进（2h+）` 黄色（**缺失**）

#### 场景 30：24h 窗口剩 < 2h
- **类型**：A + B
- **触发主体**：Meta/ycloud + CRM 倒计时
- **触发条件**：窗口剩 < 2h
- **Agent 行为**：
  1. CAVi 启动窗口即将关闭 timer
  2. 销售收到通知
  3. 销售可主动 HSM 唤醒（场景 36 配合）
- **UI 反馈**：会话项 `⏰ 窗口剩 1h 30min` 橙色警示（**缺失**）

#### 场景 31：24h 窗口剩 < 30min
- **类型**：A
- **触发主体**：CRM 倒计时
- **触发条件**：窗口剩 < 30min
- **Agent 行为**：
  1. CAVi 主动 AI 唤醒（"X 先生，窗口即将关闭，请回复..."）
  2. 仍未回复 → 标记 `window_expiring_soon`
- **UI 反馈**：会话项 `🚨 窗口剩 28min` 红色警示（**缺失**）

#### 场景 32：24h 窗口已超时（**已有**）
- **类型**：B
- **触发条件**：客户最后 inbound > 24h
- **Agent 行为**：必须用 HSM 模版才能再发消息（Meta 规则）
- **UI 反馈**：`❌ 已超时` 红色标签

#### 场景 33：7 天未联系
- **类型**：A
- **触发主体**：CRM 调度
- **Agent 行为**：自动 HSM 唤醒
- **UI 反馈**：会话项 `💤 7 天未联系` 灰色

#### 场景 34：30 天未成交
- **类型**：A
- **触发主体**：CRM 调度
- **Agent 行为**：归档到 cold_lead 池
- **UI 反馈**：会话项 `📦 已归档`

#### 场景 35：客户活跃时段画像
- **类型**：A
- **触发主体**：CAVi 历史对话分析
- **Agent 行为**：CAVi 记录客户常用回复时段
- **UI 反馈**：profile `🕐 活跃时段：20:00-23:00`（**缺失**）

#### 场景 36：客户生日 / 节日
- **类型**：A
- **触发主体**：CRM 客户档案字段
- **Agent 行为**：CAVi 自动发送节日祝福 HSM
- **UI 反馈**：profile `🎂 生日：9 月 15 日`（**缺失**）

---
### 维度 ⑤ 内容形态（9 个）

#### 场景 37：纯文字对话（**已有**）
- 标准文本气泡

#### 场景 38：客户发图片
- **类型**：B
- **Agent 行为**：
  1. ycloud Webhook 推送 media 消息
  2. CAVi 下载 + OCR（如适用）
  3. 归档到客户 documents
- **UI 反馈**：图片消息卡片 + 缩略图（**缺失**）

#### 场景 39：客户发语音
- **类型**：B
- **Agent 行为**：
  1. ycloud Webhook 推送 voice 消息
  2. CAVi STT 转文字（whisper API）
  3. 文本纳入对话历史
- **UI 反馈**：语音播放器 + 转写文字（**缺失**）

#### 场景 40：客户发文档
- **类型**：B
- **Agent 行为**：下载 + 归档
- **UI 反馈**：文档卡片 + 下载按钮（**缺失**）

#### 场景 41：客户发位置
- **类型**：B
- **Agent 行为**：调 CRM 查最近门店
- **UI 反馈**：位置消息 + 附近门店列表（**缺失**）

#### 场景 42：客户发名片
- **类型**：B
- **Agent 行为**：解析 vCard + 更新 lead phone
- **UI 反馈**：联系人卡片（**缺失**）

#### 场景 43：客户发表情/sticker
- **类型**：B
- **UI 反馈**：表情包气泡（**缺失**）

#### 场景 44：WhatsApp Flow 交互
- **类型**：A
- **触发主体**：客户点击 HSM 按钮回复
- **Agent 行为**：CAVi 解析 Flow response
- **UI 反馈**：Flow 响应气泡

#### 场景 45：客户发 unsupported 消息
- **类型**：A
- **Agent 行为**：CAVi 提示"暂不支持此类型"
- **UI 反馈**：系统提示气泡

---
### 维度 ⑥ 团队协作（8 个）

#### 场景 46：销售 A 休假，lead 转销售 B
- **类型**：B
- **触发主体**：销售手动 / 主管手动
- **Agent 行为**：
  1. CRM 更新 lead.owner
  2. 通知销售 B（站内 / WA 内部通知）
  3. 历史对话摘要发送给销售 B
- **CRM 状态变更**：`ownership_transferred`
- **UI 反馈**：会话项 `📤 转派给：李四（销售 B）` 标识 + 操作按钮（**缺失**）

#### 场景 47：销售离职，批量重分配
- **类型**：B + C（独立页面）
- **触发主体**：主管手动
- **Agent 行为**：
  1. CRM 离职销售的所有 lead 标记为 `unassigned`
  2. 主管进入批量重分配视图
  3. 按规则 / 手动分配给其他销售
- **CRM 状态变更**：批量 `unassigned` → 重新 `assigned`
- **UI 反馈**：批量操作工具栏（**缺失，待造独立页 `批量操作面板-PC.html`**）

#### 场景 48：主管质检旁观
- **类型**：B
- **触发主体**：主管手动加入
- **Agent 行为**：
  1. 主管进入只读模式
  2. 销售不知道主管在看（可配置为可见）
  3. 主管可发内部 note（不发给客户）
- **UI 反馈**：chat header `👁 主管旁观中（王经理）` + 只读标识（**缺失**）

#### 场景 49：跨团队协作（@金融顾问）
- **类型**：B
- **触发主体**：销售手动 @mention
- **Agent 行为**：
  1. CAVi 路由给金融顾问
  2. 金融顾问回复仅销售可见
  3. 销售整理后回复客户
- **UI 反馈**：chat input `@` mention 内部联系人（**缺失**）

#### 场景 50：多人同时回复占用
- **类型**：B
- **触发主体**：CAVi typing indicator
- **触发条件**：同一会话多个销售同时在线
- **Agent 行为**：CAVi 显示"X 正在输入..."
- **UI 反馈**：消息流底部 `🟢 张三正在输入...`（**缺失**）

#### 场景 51：AI 自动接管销售 idle 会话
- **类型**：A
- **触发主体**：CAVi + CRM SLA 联动
- **触发条件**：销售 idle >4h（场景27 升级）
- **Agent 行为**：
  1. CAVi 自动接管会话
  2. AI 发送续接 HSM（"您好，我是 CAVi 智能助手，张三 暂时未回复，请问我可以帮您什么？"）
  3. 通知原销售
- **CRM 状态变更**：`sales_idle` → `ai_recovery`
- **UI 反馈**：会话项 `🤖 AI 续接中（销售 idle 超时）`（**缺失**）

#### 场景 52：批量回访
- **类型**：B + C
- **触发主体**：销售手动批量操作
- **Agent 行为**：销售批量选中 → 选择回访话术模板 → 一键发送
- **UI 反馈**：批量操作工具栏（**复用批量操作面板**）

#### 场景 53：撞单检测
- **类型**：B
- **触发主体**：CRM 锁定机制
- **触发条件**：多个销售尝试回复同一 lead
- **Agent 行为**：CRM lead 锁定，首个进入会话的销售获得写权限
- **UI 反馈**：其他销售看到 `🔒 张三跟进中（只读）`（**缺失**）

---
### 维度 ⑦ 合规 / 风控 / 异常（9 个）

#### 场景 54：客户要求删除数据（LGPD）
- **类型**：B
- **触发主体**：客户 / 销售手动
- **触发条件**：客户说"删除我的信息 / 右下被遗忘权"
- **Agent 行为**：
  1. CAVi 立即标记 `gdpr_deletion_request`
  2. 通知 DPO（数据保护官）
  3. 30 天内完成删除流程
- **CRM 状态变更**：`gdpr_pending_deletion`
- **UI 反馈**：profile `🛡 合规操作` 区块 + 删除 / 导出按钮（**缺失**）

#### 场景 55：客户发送敏感信息（密码等）
- **类型**：A
- **触发主体**：CAVi 敏感 NLU
- **触发条件**：消息包含密码 / CVV / PIN 模式
- **Agent 行为**：
  1. CAVi 立即 mask 显示（"***"）
  2. 提示客户"请勿发送敏感信息"
  3. 不存储敏感字段
- **UI 反馈**：消息气泡显示 mask + 系统警告

#### 场景 56：疑似诈骗/钓鱼
- **类型**：A
- **触发主体**：CAVi 风险 NLU
- **触发条件**：客户说"你是银行吗" / "要密码吗"
- **Agent 行为**：
  1. CAVi 自动安抚："我是官方销售助手，不会索要密码"
  2. 通知合规团队
- **UI 反馈**：内部告警

#### 场景 57：Meta 24h 窗口规则触发（**已有**）
- **类型**：B
- **触发条件**：客户最后 inbound > 24h
- **Agent 行为**：必须用 HSM 模版
- **UI 反馈**：`24h 窗口 · 剩 X` 显示

#### 场景 58：HSM 配额耗尽
- **类型**：A
- **触发主体**：运营层 CAVi 配额监控
- **触发条件**：月度 HSM 配额 > 90%
- **Agent 行为**：CAVi 自动降级为客户主动 inbound 后再回复
- **UI 反馈**：销售无感知（运营层）

#### 场景 59：WA 号码被封/客户封了我们
- **类型**：A
- **触发主体**：ycloud Webhook status
- **触发条件**：message status = blocked / failed
- **Agent 行为**：CAVi 标记 lead `unreachable`，通知销售改用其他渠道
- **UI 反馈**：会话项 `🚫 号码不可达`

#### 场景 60：同一客户多个 WA 号码
- **类型**：A
- **触发主体**：CRM 客户匹配算法
- **触发条件**：phone 匹配到同一身份证号 / 邮箱
- **Agent 行为**：CRM 合并 lead
- **UI 反馈**：profile `📱 主号：+52... / 备用：+52...`

#### 场景 61：客户说错语言
- **类型**：A
- **触发主体**：CAVi 多语言路由
- **触发条件**：客户用英语 / 中文回复（系统主语言是西语）
- **Agent 行为**：
  1. CAVi 检测语言
  2. 切到对应语言模型（GPT-4 EN 等）
  3. 销售无感知
- **UI 反馈**：会话项 `🌐 语言：English`

#### 场景 62：客户 opt-out（拒绝接收）
- **类型**：A
- **触发主体**：客户 / CAVi 检测
- **触发条件**：客户说"不要再发了 / stop / unsubscribe"
- **Agent 行为**：
  1. CAVi 立即标记 `opted_out`
  2. 停止所有 outbound
  3. 销售仅可在客户主动 inbound 时回复
- **UI 反馈**：会话项 `🚫 已退订` 灰色（**缺失**）

---

## 三、统计总览

| 维度 | 场景数 | A 底层规则 | B 可视化功能 | C 独立页面 |
|---|---|---|---|---|
| ① 会话来源 | 8 | 4 | 4 | 0 |
| ② 会话主体 | 6 | 1 | 4 | 1 |
| ③ 客户行为/意图 | 12 | 7 | 5 | 0 |
| ④ 时间维度 | 10 | 8 | 2 | 0 |
| ⑤ 内容形态 | 9 | 2 | 7 | 0 |
| ⑥ 团队协作 | 8 | 1 | 6 | 1 |
| ⑦ 合规风控 | 9 | 7 | 2 | 0 |
| **合计** | **62** | **30** | **30** | **2** |

**当前已有 UI 覆盖**：约 10 / 30 个 B 类 = **33%**
**待补可视化功能**：20 个（修改现有页面） + 2 个独立新页面

---

## 四、优先级路线图

### P0（必须先做）
1. **场景 27** —— 销售 idle >1h（**用户核心场景**）
2. **场景 2** —— 陌生 WA lead（**用户核心场景**）
3. **场景 1** —— 现有客户留资 AI 主动（**已有基础**）
4. **场景 47** —— 销售离职批量重分配（**独立页面**）

### P1（重要）
- 场景 17（犹豫）、20（试驾）、24（投诉）、46（转派）、51（AI 续接）

### P2（运营/合规）
- 场景 54-62

---

## 五、与现有文档的关系

- **`WhatsApp Agent/CAVi AI Sales Agent 主动跟进规划.md`**（v0.4）：
  - 业务全景 + ycloud n8n 集成架构
  - 本文档是该文档**场景维度的细化版**
- **`销售agent/统一客服收件箱-PC.html`**：
  - 销售可视化操作界面
  - 本文档的 B 类场景对应 HTML 落地

---

## 变更日志

- **v0.1**（2026-09-01）：
  - 创建文档
  - 7 维度穷举 62 个场景
  - A/B/C 三类分层（底层规则 / 可视化功能 / 独立页面）
  - 用户提的 3 个场景（线索主动 WA / 陌生主动 / 销售 idle）已纳入对应维度
